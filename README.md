# Postman AIML Task

## Running The Project

Just install the dependencies as mentioned
The SSM and Mamba implementations reside in `ssm.py` and `mamba.py` respectively.
The stability analysis (2.3) resides in `stability.py`, which generates `stability.png`.
